# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Paid/licensed art assets — must NOT be committed (keep the README as a marker)
src/assets/sprites/*
!src/assets/sprites/README.md

# Derived sprite assets (e.g. favicon cropped from a paid spritesheet) — also
# licensed art, so kept out of the public repo. Supplied to the deploy separately.
public/cat-favicon.png

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
